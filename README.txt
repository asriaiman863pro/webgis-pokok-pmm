WEB GIS PMM V59

UPS update map now follows the UPS search exactly. Searching UID, ID Pokok, or Petak changes both the result list and the points shown on the UPS mini-map. Reset restores all points. Each UPS point uses the same status colours as the main map and is clickable to show UID, ID Pokok, Nama Pokok, Petak, Status, plus a button to open the update editor. UAV remains as the 100% overlay. Existing navigation and data functions are preserved.
